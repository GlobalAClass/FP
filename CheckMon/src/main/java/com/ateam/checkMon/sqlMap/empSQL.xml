<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="checkMon.emp">
	<insert id="empInsert" parameterType="com.ateam.checkMon.emp.model.EmpDTO">
		insert into 
			employee
		values(
			employee_idx.nextval,
			#{name},
			#{email},
			#{dept}
		)
	</insert>
	<delete id="empDelete" parameterType="String">
		Delete from employee where name = #{name}
	</delete>
	<select id="empList" parameterType="com.ateam.checkMon.emp.model.EmpDTO" resultType="com.ateam.checkMon.emp.model.EmpDTO">
		select * from employee
	</select>
	<select id="empSelect" parameterType="String" resultType="com.ateam.checkMon.emp.model.EmpDTO">
		select * from employee where name = #{name}
	</select>
	<select id="empModify_form" parameterType="int" resultType="com.ateam.checkMon.emp.model.EmpDTO">
		select * from employee where idx = #{idx}
	</select>
	<update id="empModify" parameterType="com.ateam.checkMon.emp.model.EmpDTO">
		update employee set name=#{name}, email=#{email}, dept=#{dept} where idx = #{idx}
	</update>
</mapper>